double d;

void main()
{
double loc;
d = 4.5;
loc = 5.5;
printf("local: %.1f, global: %.1f\n", loc, d);
loc = d;
printf("local from global: %.1f\n", loc);
d = loc;
printf("global from local: %.1f", d);
}